services:
  backend:
    build: .
    container_name: projet-mistral-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./frontend:/app/frontend
    restart: always
